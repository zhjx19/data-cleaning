# Changelog

格式参考 [keepachangelog](https://keepachangelog.com/) 精神：每条讲清"为什么改"，不只"改了什么"。

## [1.0.0] — 2026-09-13 首次发布

### 核心能力
- **先审计后清洗工作流**：单文件 6 步（审计摘要 → 问题清单 → 清洗 → 清洗后验证 → 交付）、多文件 6 步（独立审计 → 候选键 → 键一致性五查 → 关系判定 → join 选型正向规则 → 双向 anti_join/抽样核对验证）。
- **高风险决策防线**：主键不明、N:N 连接、口径冲突、异常值删除、大量缺失删除必须暂停确认；"用户催办不豁免"反操控条款 + 用户书面坚持的升级出口（日志标 `用户强制`，唯覆盖原始文件绝对拒绝）。
- **缺失值三档量化决策线**：<5% 非关键列可默认填补 / 5%–40% 保留 NA 询问 / >40% 或关键列交用户拍板。
- **异常值三法并列**：IQR（默认）/ z-score（正态大样本）/ MAD（重尾小样本），`across(.names=)` 逐列打标无需 for 循环。
- **join 诊断**：关系判定（1:1/1:N/N:1/N:N）+ 键一致性五查（类型/前导零/空格/大小写折叠差）+ `match_rate` 跨表对比 + 连接后双向未匹配验证。
- **结构探测**：汇总行剥离、合并单元格结构性缺失（`fill()` 继承，不进缺失三档线）、类型断言（`where(is.numeric)` 对 character 列静默跳过的防护）。
- **三大交付物**：`*_cleaned` 数据（原始永不覆盖）+ 结构化清洗日志（每步一行）+ PrettyTypst 排版质量报告 PDF（vendored 扩展，中文跨平台渲染，离线可用）。
- **执行协议**：Rscript + JSON（stdout 纯 JSON）、GBK/GB18030 静默乱码判码（`read_csv_anyenc`）、Windows 全套坑表（BOM/locale/编码/渲染）。

### 质量
- 43 项回归全绿：`scripts/verify_snippets.R`（11 组片段 + 模板禁用词 lint，任意 locale 可跑）+ 端到端示例（43 行做脏数据 → 清洗日志 → PDF 报告）。
- 红队两轮推演驱动：缺失三档线、键一致性三层闸、结构探测、跨 runtime 中性化均由推演发现并修复。
- 姊妹技能 `tidy-data`（数据思维 8 范式）双向契约：清洗涉及 reshape/分组/累计时路由兜底，两份 R 铁律同步维护。
